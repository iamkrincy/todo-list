<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
   h1{
     border: 1px solid;
    }

    input{
     margin: 10px;
    }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
    background-color: #f7f7f7;
    background-image: linear-gradient(to bottom, #f7f7f7, #ffe6cc);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  div[align="center"] {
    width: 80%;
    margin: 40px auto;
    text-align: center;
    background-color: #fff;
    padding: 20px;
    border: 1px solid #ddd;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  }

  h1, h3 {
    margin-bottom: 10px;
    color: #091185;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    border: 1px solid #ddd;
  }

  th, td {
    border: 1px solid #ddd;
    padding: 10px;
    text-align: left;
  }

  th {
    background-color: #f7f7f7;
    color: #090083;
  }

  tr:nth-child(even) {
    background-color: #f2f2f2;
  }

  input[type="text"], input[type="datetime-local"] {
    width: 100%;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ccc;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  }

  input[type="submit"] {
    background-color: #011f81;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 10px;
    cursor: pointer;
  }

  input[type="submit"]:hover {
    background-color: #087c3c;
  }

  /* Add some animations */

  div[align="center"] {
    animation: fade-in 1s;
  }

  @keyframes fade-in {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }

  input[type="submit"] {
    transition: background-color 0.5s;
  }

  input[type="submit"]:hover {
    transition: background-color 0.5s;
  }
</style>
    </style>

</head>
<body>

    <div align="center">

    <h3>ADD TASK </h3>
    <form action="/update" method="post">
        <table border="1" class="tbl">
            <thead>
                <tr>
                    <th>Task :-
                        <input type="text" placeholder="Enter Task" value="<%= single.task %>" name="task">
                    </th>
                </tr>

                <tr>
                    <th>Status :-
                        <input type="text" placeholder="Enter Status" value="<%= single.status %>" name="status">
                    </th>
                </tr>

                <tr>
                    <th>Deadline :-
                        <input type="datetime-local" value="<%= single.deadline %>" name="deadline">
                    </th>
                </tr>

                <tr>
                    <th>
                        <input type="hidden" name="id" value="<%= single.id %>">
                        <input type="submit" value="Update Task">
                    </th>
                </tr>
            </thead>
        </table>
    </form>

    </div>

</body>
</html>